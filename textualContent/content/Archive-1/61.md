## Javascript Gym ⚡️⚡️

Here is a pretty clever use of **bit operator** to make your code more **elegant** 🥇 

 ```javascript 
 var str = 'rawr';
var searchFor = 'a';

// This is alternative way of typing if (-1*str.indexOf('a') <= 0)
if (~str.indexOf(searchFor)) {
  // searchFor is in the string
} else {
  // searchFor is not in the string
}
  ```
> #61 